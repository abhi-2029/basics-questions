#include<iostream>
using namespace std;
int main(){
    int length;
    int breadth;
    int area;
    cout<<"Enter the value of length: ";
    cin>>length;
    cout<<"Enter the value of breadth: ";
    cin>>breadth;
    area=length*breadth;
    cout<<"Area of rectangle: "<<area<<endl;

    return 0;
}